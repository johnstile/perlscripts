Very Short setup:

Create cron run svn_trck_repo_growth.pl, which loads data into mysql database.

Configure this directory to run svngrowth.cgi as a cgi, which gives access to the mysql database data.

main_jquery.js plots the data
